<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.compare.pcparts.mapper.PcAndPartsMapper">
    <insert id="insertCpu" >
        insert into cpu
        (
        id,
        itemname,
        storeid,
        isavailable,
        price,
        url,
        imageref
        )
        values(
        default,
        #{itemname},
        #{storeid},
        #{isavailable},
        #{price},
        #{url},
        #{imageref}
        )
    </insert>
</mapper>